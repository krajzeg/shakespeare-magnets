#!/usr/local/bin/node

var magnetsServer = require('../dist/src/server')();

var port = process.env.PORT || 3000;
magnetsServer.listen(port, function() {
  console.log("Server started on port " + port + ".");
});
